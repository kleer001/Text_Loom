from textual.theme import Theme

default_theme = Theme(
    name="default",
    dark=False,
    
    # Core colors
    background="#FAFAF8",    # Main background color
    foreground="#222222",    # Main text color
    primary="#486591",       # Medium slate blue - main accent
    secondary="#2C4975",     # Deep slate blue - secondary accent
    accent="#EDF2F7",       # Light blue-gray selection
    error="#D93025",        # Error red
    success="#4CAF50",      # Success green (added for completeness)
    warning="#FFA726",      # Warning orange (added for completeness)
    surface="#F5F5F2",      # Modal and overlay surfaces
    panel="#FFFFFF",        # Input backgrounds and panels
    
    variables={
        # Text colors
        "text-muted": "#444444",
        "text-on-primary": "#FFFFFF",
        "text-on-secondary": "#FFFFFF",
        
        # Border styles (preserving original structural styles)
        "border-primary": "solid $primary",
        "border-secondary": "solid $secondary",
        "border-focus": "double $secondary",
        "border-modal": "thick $primary",
        
        # Background variations
        "background-input": "$panel",
        "background-input-selected": "$accent",
        "background-input-invalid": "rgba(217, 48, 37, 0.1)",
        "background-editing": "$primary 10%",
        "background-help": "$primary 5%",
        
        # Component-specific
        "modeline-bg": "$secondary",
        "modeline-fg": "$text-on-secondary",
        
        "node-selected-bg": "$accent",
        "node-selected-fg": "$foreground",
        
        "param-title": "$secondary",
        "param-label-bg": "$surface",
        "param-label-fg": "$text-muted",
        
        # Input states
        "input-border": "#D0D0D0",
        "input-border-focus": "$primary",
        "input-border-invalid": "$error",
        
        # Window borders
        "window-border-normal": "$border-primary",
        "window-border-focus": "$border-focus",
        
        # Table colors
        "table-border": "$primary",
        "table-header-bg": "$primary 15%",
        "table-alternate-bg": "$surface",
    }
)
